{
  "compilerOptions": {
    /* ► Saída (JS) */
    "target": "ES2020",
    "module": "ESNext",          // gera ES-modules nativos

    /* ► Resolução que aceita import “…/arquivo.js” vindo de .ts */
    "moduleResolution": "bundler", // mantém extensão .js nos imports
    "verbatimModuleSyntax": true,  // não altera a string do import

    /* ► Ambiente */
    "lib": ["ES2020", "DOM", "DOM.Iterable"],

    /* ► Estrutura do projeto */
    "rootDir": "./src",
    "outDir":  "./FrontEnd/dist",

    /* ► Regras / verificações */
    "strict": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true
  },

  "include": ["src/**/*"],
  "exclude": [
    "node_modules",
    "FrontEnd/dist"
  ]
}
